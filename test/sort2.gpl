void main() {
    graph g = [ a -(0)> b -(4)> c -(3)> d; d -(1)> e -(2)> f;];
    edge[5] edges = g.getAllEdges();
    sortEdges(edges);
    int i;
    for(i = 0; i<edges.len(); i+=1) {
    	print(edges[i].getWeight());
    }
}

void sortEdges(edge[] e) {
	e.sort();
}
